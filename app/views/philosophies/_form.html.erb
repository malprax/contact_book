<%= form_for(@philosophy) do |f| %>
  <% if @philosophy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@philosophy.errors.count, "error") %> prohibited this philosophy from being saved:</h2>

      <ul>
      <% @philosophy.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div  class= "field">
    <%= f.label :input_text %><br>
    <%= f.text_area :input_text, :class => "wysihtml5",  :id => "some-textarea" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
